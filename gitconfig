[user]
	email = xsawyerx@cpan.org
	name  = Sawyer X

[alias]
    st  = status
    ci  = commit
    br  = branch
    co  = checkout
    df  = diff --color
    lg  = log -p --color
    who = shortlog -s --
    lp  = log --graph --pretty=format:'%Cred%h%Creset — %s %Cgreen(%cr)%Creset %an' --abbrev-commit --date=relative --color

